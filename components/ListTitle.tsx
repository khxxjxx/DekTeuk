import styled from '@emotion/styled';

const ListTitle = () => {
  return <MainTitle>자유게시판</MainTitle>;
};

export default ListTitle;

const MainTitle = styled.div`
  border-bottom: 1px solid #8946a6;
`;
